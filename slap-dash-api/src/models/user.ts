export interface User {
  id: number; // guid
  name: string; // not going to inforce this to be unique but probably should be
  // ipAddress: string; // ip address of the user - this will be used to track the user and to potentially prevent spam
  // primaryIdentityId: // if allowing for exising user stuff
};
